<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Cinephile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Cinephile.Views"
    Title="Cinephile"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Icon="home.png">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate views:UpcomingMoviesListView}"
                Route="MainPage" />
        </Tab>
        <Tab Icon="settings.png">
            <ShellContent
                Title="About"
                ContentTemplate="{DataTemplate views:AboutView}"
                Route="About" />
        </Tab>
    </TabBar>
</Shell>
